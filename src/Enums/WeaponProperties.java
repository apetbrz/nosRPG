package Enums;

public enum WeaponProperties {
    LIGHT,          //can be off-handed
    FINESSE,        //may use DEX instead of STR (if higher)
    TWO_HANDED,     //uses both hand slots
    VERSATILE       //can be two-handed (for a bonus)
}
